# Audio files belong on archive.org, not in the website repo
*.mp3
*.wav
*.aif
*.aiff

# LaTeX source files belong in the source folder, not in the website repo.
# Existing tracked .tex files (e.g., under Belle_Deacon/) are unaffected;
# this only prevents new ones from being added accidentally.
*.tex

# OS / editor noise
.DS_Store
Thumbs.db
